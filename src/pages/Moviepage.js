import MoviesList from "../components/MoviesList";

export default function Moviepage(){
    return(
        <div>

            <h2 className='text'>All movies</h2>
            <MoviesList/>
        </div>
    );
}